# Turn 3 — Arena handoff smoke test

This is an end-to-end handoff test.

Arena must:

1. Read `.agent-handoff/PROTOCOL.md` and `.agent-handoff/signal.json` from branch `agent-handoff`.
2. Confirm that the current signal is `turn_id: 3`, `target: ARENA`, `status: ready`.
3. Create or update durable GitHub output for this test with the exact acknowledgement `ARENA_HANDOFF_OK turn_id=3`.
4. After the durable output exists, advance `.agent-handoff/signal.json` to `turn_id: 4`, `target: GPT`, `source: ARENA`, `status: ready`, with a short message telling GPT where the acknowledgement was written.
5. Do not touch `main` just to signal the next agent.

If Arena cannot write to GitHub, it must say so explicitly in its chat response and must not pretend that steps 3–4 succeeded.
