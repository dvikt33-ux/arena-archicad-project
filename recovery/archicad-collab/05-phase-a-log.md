# Фаза A — журнал (Tapir-only прототип)

## Шаг A-1. Скелет демона 0.1.0 (2026-09-11) ✅

**Что реализовано:**
- `src/accollab/connector.py` — клиент AC29+Tapir (stdlib only): официальные `API.*`,
  Tapir через `ExecuteAddOnCommand`, хелперы (GUID, типы, детали, выборка, BBox, подсветка, ping).
- `src/accollab/store.py` + `store_schema.sql` — SQLite (WAL): projects, participants,
  elements_cache, operations (идемпотентность по change_id), outbox, locks, conflicts,
  comments, checkpoints, sync_state.
- `tests/mock_archicad.py` — фейковый HTTP-сервер, повторяющий живой формат AC29.
- `tests/test_connector.py` — 10 тестов (коннектор + идемпотентность Store).

**Что проверено:** 10/10 тестов зелёные (`cd tests && python3 -m unittest test_connector`).
Протокол коннектора = живой протокол (подтверждён `evidence/diag-2026-09-11-v2.json`).

**Что НЕ проверено:** коннектор против живого AC29 (только мок); HighlightElements вживую;
поведение на 10k+ элементов; переподключение при рестарте Archicad.

**Ограничения, обнаруженные тестом:** нет (мок повторяет живой формат).

**Как запустить:** `cd tests && python3 -m unittest test_connector -v` (нужен только Python 3).

**Как откатить:** удалить `src/` и `tests/` — остальные файлы проекта не зависят от них.

## Живой тест №1. Подсветка 3 балок (2026-09-11) ✅
- Скрипт `tools/highlight_test.py` (вариант B — вставка текста из чата, т.к. paste.rs
  заблокирован в сети заказчика, а `curl` в PowerShell 5.1 — алиас; впредь `curl.exe`).
- Результат: консоль `Beam id="БЛК - 001/002/003"` + `OK`, на плане 3 объекта синие.
- Доказательства: `evidence/highlight-plan-blue.png`, `evidence/powershell-highlight-success.png`.
- Вывод: HighlightElements работает вживую, проект не меняется, спор «балки или линии» закрыт.

## Шаг A-2. Watcher + первая операция (2026-09-11) ✅ код готов

**Что реализовано:**
- `src/accollab/watcher.py` — снимки (GUID→тип+checksum+детали, батчи по 200),
  diff (created/deleted/modified), запись операций modify/create/delete с before/after
  в Store + outbox. Первый прогон = baseline без операций.
- `store.py` — outbox (enqueue/list), snapshots (save с ротацией 20/load_last).
- `store_schema.sql` — таблица snapshots.
- `tests/test_watcher.py` — 4 теста (чистый diff, baseline, мутация→1 modify с before/after, тишина→0 ops).
- `tools/diff_test.py` — живой тест №2 (снимок → ручной сдвиг балки → diff с координатами).
  Зеркало: https://d.uguu.se/fttEhZhq.py (sha256 сверен).

**Что проверено:** 14/14 тестов зелёные (10 коннектор + 4 watcher).

**Что НЕ проверено:** предположение A-2.1 (порядок деталей = порядку GUID) — проверяется
живым тестом №2; скорость снимка 393 балок вживую; поведение при несохранённых изменениях
(ожидаем: API видит модель в памяти, сейв не требуется).

**Как запустить:** `cd tests && python3 -m unittest test_connector test_watcher`
**Как откатить:** удалить `src/accollab/watcher.py`, `tests/test_watcher.py`, `tools/diff_test.py`;
таблица snapshots безвредна (CREATE IF NOT EXISTS).

## Живой тест №2. Ручной сдвиг балки → diff (2026-09-11) ✅
- Скрипт `tools/diff_test.py` (вариант B — вставка из чата).
- Результат: `created=0 deleted=0 modified=1`, поймана `Beam id="БЛК - 151"`,
  `OK: diff nayden`. Первые ID идут по порядку БЛК-001…005 — позиционное
  соответствие деталей (A-2.1) подтверждено.
- Нюанс: строк координат в выводе нет — позже выяснилось, что скрипт читал beg/end
  из корня вместо details.* (та же ошибка, что в тесте №3 v1.1). Checksum изменение поймал.
- Доставка файлов: paste.rs заблокирован, `curl.exe` на машине отсутствует —
  рабочий канал сейчас только вставка текста из чата. GitHub из сети заказчика
  доступен (Tapir/DevKit качались оттуда) — будущие пакеты отдавать через GitHub-релизы.
- Доказательство: `evidence/powershell-diff-test2-success.png`.

## Шаг A-3. Запись в проект через API (2026-09-11) ✅ код готов
- `tools/api_move_test.py` — сдвиг балки на (+0.5, 0, 0) через Tapir MoveElements
  (copy=false), сверка координат, подтверждение, отмена Ctrl+Z.
  Зеркало v1.2: https://n.uguu.se/VOUYQiOV.py (sha256 сверен).
- Заказчик подтвердил готовность («готово», копия проекта сохранена — на его стороне).

## Живой тест №3. Сдвиг балки через API (2026-09-11) ✅
- Попытка №1: `TimeoutError: timed out` (доказательство: `evidence/powershell-apimove-timeout.png`).
  Archicad принял соединение, но не ответил за 120 с. Вывод для демона: обязательны
  таймауты + ретраи + статус «Archicad занят».
- Попытка №2: снова TimeoutError → скрипт v1.1 (автоскан портов, STOP-отчёт).
- Ответы заказчика: окно отвечает, диалогов нет, инстанс один, сохранения нет.
- Попытка №3 (v1.1): ПРОРЫВ. Скан: `19723: MOLChIT`, `19724: OK (AC 29.3000 RUS)` —
  Archicad уплыл на 19724, на 19723 зомби-процесс. Автоскан портов обязателен в демоне.
  MoveElements вернул `success:true`, но сверка показала пустые beg/end — ОШИБКА СКРИПТА
  (читали корень вместо details.*). Исправлено в v1.2. Вывод: `evidence/apimove-v11-output.txt`.
- Попытка №4 (v1.2): ✅ ТЕСТ ПРОШЁЛ. Цель БЛК-200 найдена по суффиксу,
  dx=0.5000 dy=0.0000 — точное совпадение. Доказательство: `evidence/powershell-apimove-v12-ok.png`.
  Состояние проекта заказчика: БЛК-201 +1.0, БЛК-200 +0.5 (для чистоты — Ctrl+Z ×3).
  Попутно: подтверждение словом MOVE путает (вводили команду запуска в prompt) —
  впредь Y/N.

## Шаг A-4. Applier + перенос операций файлом (2026-09-11) ✅ код готов

**Что реализовано:**
- `src/accollab/applier.py` — применение операции modify: base check (checksum),
  вектор из дельты координат (держит обе вложенности details), MoveElements,
  mark_applied. ConflictError (нет элемента / base не совпал), UnsupportedOpError.
- `src/accollab/transfer.py` — export/import envelope v1.0, идемпотентный импорт.
- `store.mark_applied()`.
- `tests/test_sync.py` — 5 тестов: математика вектора, полный цикл
  record→transfer→apply, конфликт base, конфликт missing, unsupported.
- Найден и исправлен баг двойной вложенности details (item.details.details).

**Что проверено:** 19/19 тестов зелёные (10 коннектор + 4 watcher + 5 sync).

**Что НЕ проверено:** живой двухмашинный цикл — назначен на понедельник 2026-09-14
(чек-лист: `06-second-machine.md`). Полевые инструменты `ops_export.py`/`ops_apply.py` —
выдать в понедельник утром.

**Как запустить:** `cd tests && python3 -m unittest test_connector test_watcher test_sync`

## Шаг A-4б. Полевые инструменты ops_export/ops_apply + репетиция (2026-09-12) ✅

**Что реализовано:**
- `tools/ops_export.py` v1.0 (машина A): снимок → baseline → diff → `ops.json`
  (конверт protocol 1.0, схема операций = Store). Только чтение из Archicad.
- `tools/ops_apply.py` v1.0 (машина B): проверка протокола, base check,
  план + подтверждение Y/N, MoveElements, сверка координат (eps 1e-3),
  `ops_report.json`, идемпотентность (SKIP-dubl). Конфликты проект не трогают.
- `tests/mock_archicad.py`: координаты балок, MoveElements, независимое
  состояние на порт (эмуляция двух машин). 19/19 тестов по-прежнему зелёные.
- `tests/sim_two_machines.py`: репетиция понедельника — 9/9 шагов прошли
  (baseline → сдвиг → export → перенос → APPLIED → A==B → дубль →
  CONFLICT-base без касания проекта → отмена по N).
- Кит v4: +ops-инструменты, `03-export.bat`, `04-apply.bat`, инструкция
  дополнена разделом синхронизации. sha256 `764a157e…`.

**Что НЕ проверено:** живой двухмашинный цикл на реальных AC29 (понедельник).
Риск: float-округление живых координат (заложен eps + checksum-варнинг, не фейл).

## Шаг A-5. Единая система: демон v0.2.0 (2026-09-12) ✅

**Что реализовано:**
- `store.py` v2 (миграция locks.mode) + конфликты, чекпоинты, sync_state, dequeue_outbox.
- `locks.py` — soft/hard, аренда+heartbeat+протухание, remote lock/unlock операции.
- `applier.py` v2 — диспетчер apply_with_report, hard-блок запрещает чужие modify,
  конфликты (missing/base/locked) пишутся в журнал с code.
- `checkpoints.py` — чекпоинт + откат компенсирующими modify через штатный applier,
  честные skipped (created/deleted-after, no-coords-vector).
- `relay.py` — ретранслятор для интернета: push/fetch курсором, дедуп, секрет,
  персистентность jsonl, CLI-сервер. Клиент с RelayError.
- `daemon.py` — тик (watch→export→relay→import→apply→heartbeat→status.json),
  файловый транспорт (.done/.bad), relay_sync с курсором, CLI (13 команд).
  Без Archicad тик не падает (офлайн-очередь живёт).
- Тесты: test_locks (6), test_checkpoints (3), test_relay (4), test_daemon (5).

**Что проверено:** 37/37 зелёные, ворнингов нет; sim_two_machines 9/9 по-прежнему.
По пути пойманы и исправлены: SQL-опечатка (6/7 колонок), гонка транспортов
(файлы осушали outbox раньше relay — теперь при relay файлы только аудит).

**Что НЕ проверено:** демон против живого AC29; relay через реальный интернет;
долгая работа run (утечки/переподключения); гонки при 3+ участниках.

**Руководство:** `07-daemon.md`. В кит понедельника демон НЕ входит
(там полевые tools; демон пакуется позже в EXE на Windows-сборке).

## Шаг A-6. Установщик v0 (пакет сборки, 2026-09-12) ✅ скрипты готовы, EXE не собран

**Что реализовано (всё в `installer/` + `.github/workflows/`):**
- `build.bat` — сборка одной командой на Windows: Python check → PyInstaller →
  скачивание Tapir 1.5.9 → `accollab.exe` → Inno Setup → `ACCOLLAB-Setup-0.2.0.exe` + sha256.
- `app_main.py` + `accollab.spec` (onefile, console, store_schema.sql в бандле) +
  `version_info.txt` (0.2.0).
- `setup.iss` (Inno 6): per-user без админа, EN/RU мастер, задача Tapir
  (копия в Add-Ons + fallback-инструкция), проверка AC29 (warn, не блок),
  ярлыки (once/status/инструкция), создание конфига, тихий режим штатно.
- `build-windows.yml` — GitHub Actions: тесты → EXE → Setup → Artifact/Release.
- `tapir-LICENSE.txt` (MIT — bundling разрешён, текст приложен по условиям),
  `USTANOVKA.txt` (BOM+CRLF), `installer/README.md` (2 пути сборки + матрица v0).
- В демон добавлена команда `relay-serve` (ретранслятор из того же EXE).

**Что проверено:** синтаксис spec/entry/version (ast), YAML workflow валиден,
bat/iss — чистый ASCII, 37/37 тестов зелёные после добавления команды.

**Что НЕ проверено (нужен Windows):** реальная сборка EXE/Setup, запуск
установщика по матрице v0 (8 пунктов), SmartScreen, удаление, повторная установка.

**Позиция заказчика:** приоритет — «лишь бы работало», происхождение кода
неважно. По факту конфликта нет: Python (PSF) и Tapir (MIT) свободно
распространяемы; в установщике только они + наш код.

## Шаг A-7. Telegram-транспорт (2026-09-12) ✅

**Правило заказчика (постоянное):** добавлять всё, что можно добавить;
вопросы лицензий/распространения работу не останавливают.

**Что реализовано:**
- `telegram_transport.py` — push (куски до 3500 символов с шапкой
  `ACCOLLAB batch i/n`), fetch_raw (getUpdates + offset), parse/merge
  с персистентным буфером, 429→retry_after→повтор. Только stdlib.
- Демон: конфиг telegram_bot_token/chat_id/api_base, telegram_sync в тике
  (push outbox + fetch + импорт + курсор update_id), status.json расширен.
  Файловый экспорт гасит очередь только если сетевых транспортов нет.
- Тесты: test_telegram.py (5: roundtrip, куски вразбивку, 429, чужой токен,
  мусор) + test_daemon.test_telegram_flow (A→канал→B, APPLIED, координаты).
  Фейковый Bot API на localhost.

**Что проверено:** 43/43 зелёные. По пути пойман баг парсинга пути
в фейковом сервере (split на 3 части вместо 2).

**Что НЕ проверено:** живой Bot API (нужны токен+канал заказчика);
длинные партии 10+ сообщений; два демона пишут одновременно (интерливинг
партий поддержан буфером, но живьём не гонялся).

## Шаг A-8. Командный режим (2026-09-12) ✅

**Задача заказчика:** соединение должно держать целую команду разработчиков
и не конфликтовать.

**Что реализовано:**
- Telegram: fetch до 5 страниц/тик (всплески), протухание оборванных партий
  (prune 1 час), HMAC-подпись партий (telegram_secret, подделки отбрасываются
  со счётчиком forged_dropped).
- Демон: детерминированный порядок применения (lamport, change_id) — все машины
  выбирают одного победителя; предупреждения о правках под чужим hard-блоком
  (rep + status.json warnings).
- Починены утечки fd: HTTPError-ответы теперь закрываются (connector, relay,
  telegram) — важно для суточного `run`.
- Тесты: +5 (4 писателя вперемешку, prune, подписи end-to-end, детерминированный
  победитель, warnings в тике).

**Что проверено:** 48/48 зелёные, ResourceWarning нет.

**Командный регламент (минимум):** один приватный канал на проект; общий
telegram_secret у всех; перед долгой правкой — `lock <guid> --mode hard`;
конфликты разбираются через `conflicts`/`resolve`, авто-разрешения нет.

**Что НЕ проверено:** живой канал с 3+ писателями; гонка lock-операций
(пришла позже, чем правка) — лечится регламентом + warnings.

## Шаг A-9. Комментарии + присутствие + бэкап + история (2026-09-12) ✅

**Режим заказчика (постоянный):** автономный цикл без вопросов —
тест → правка → тест → новые функции → регресс старых.

**Что реализовано:**
- `comments.py` — add/reply/resolve локально + операции kind=comment
  в общем транспорте; applier-диспетчер (COMMENT-APPLIED/DUP/REPLIED/RESOLVED).
- `presence.py` — подсветка локов цветами holder'ов (только временная,
  вызов на holder'а, без локов — тишина); хук в тике (только при изменении
  набора локов, тик не роняет).
- `backup.py` — verify-then-rotate копий PLN (каталог внутри Яндекс Диска =
  облако само); чужие файлы не трогает.
- Store.ops_for_element; CLI: comment/comments/comment-reply/comment-resolve,
  history, backup (путь проекта из Tapir GetProjectInfo).
- Тесты: test_comments (4), test_presence (3), test_backup (4),
  test_daemon.test_cli_comments_history_backup.

**Регресс:** новый хук присутствия сломал 2 старых теста (у FakeConn не было
highlight — мок отстал от коннектора). Мок дотянут, не код.

**Что проверено:** 60/60 зелёные; sim_two_machines 9/9.

**Что НЕ проверено:** подсветка вживую (цвета/затирание чужой подсветки);
GetProjectInfo.projectPath на живой связке (ключ из живого diag v2);
бэкап больших PLN (скорость хэширования).

## Шаг A-10. BIM-контроль + страница статуса (2026-09-12) ✅

**Что реализовано:**
- `bim_checks.py` — DUP_ID / EMPTY_ID по снимку (советуют, не блокируют);
  CLI `bim-check` (exit 3, если есть замечания).
- `statuspage.py` — localhost-страница: кнопка «Синхронизировать», карточки,
  блоки, конфликты с формами закрытия, чекпоинты + форма. Русский UTF-8,
  стили inline, внешних ресурсов нет. CLI `serve --port 8472`.
- `Daemon.make_checkpoint()` — метод вместо дублирования в CLI/странице.
- Тесты: test_bim (3), test_statuspage (4: GET, POST sync/resolve/checkpoint).

**Пойман баг потоков:** SQLite-соединение создавалось в main, использовалось
в потоке сервера → ProgrammingError. Лечение: check_same_thread=False +
однопоточный HTTPServer (запросы сериализованы). Relay на памяти — не затронут.

**Что проверено:** 67/67 зелёные; sim_two_machines 9/9.

**Что НЕ проверено:** страница в живом браузете; POST /sync при долгой
модели (таймаут браузера раньше тика — лечится фоном, пока синхронно).

## Шаг A-11. Фоновый /sync + материалы в BIM (2026-09-12) ✅

**Что реализовано:**
- Страница: POST /sync идёт в фоне (single-flight, своё соединение с БД,
  SQLite timeout=30 сериализует писателей), баннер «идёт...», итог — в msg.
- BIM: правило NO_MATERIAL (поле buildingMaterialId подтверждено живым diag v2,
  держит обе вложенности details); counts — только ненулевые коды.
- Попутно: docstring CLI приведён к реальным 19 командам; печать bim-check
  обобщена под новые коды.
- Слои по именам ОТЛОЖЕНЫ честно: в живом diag есть только layerIndex,
  имён нет; API.GetLayerAttributes живьём не зондировали — гадать не стал.

**Что проверено:** 68/68 зелёные; sim_two_machines 9/9.

**Что НЕ проверено:** два POST /sync впритык при долгой модели (single-flight
покрыт кодом, гонка живьём не ловилась); страница в живом браузере.

## Шаг A-12. Пин порта + сквозная репетиция по HTTP (2026-09-12) ✅

**Что реализовано:**
- Конфиг `archicad_port` (0 = автоскан, иначе один порт) — урок зомби на 19723:
  при нескольких инстансах демон цепляется ровно к своему. Все 6 вызовов скана
  идут через `Daemon.scan()`.
- `tests/sim_daemon_http.py`: два mock Archicad на настоящих 19723/19724,
  два демона с пином — baseline, сдвиг→файл→APPLIED→сверка координат,
  hard-lock, комментарий, presence, status.json. 6/6 шагов.

**Урок инструментов:** параллельные правки одного файла затёрли друг друга
(из 6 замен встали 3) — один файл правится строго последовательно.

**Что проверено:** 68/68 зелёные; sim_two_machines 9/9; sim_daemon_http 6/6.

**Что НЕ проверено:** пин против живых двух инстансов AC (26+29 рядом);
порты 19723/19724 на сборочной машине (там настоящий Archicad, репетиция
откажется честным SKIP).

## Шаг A-13. Дизайн страницы в духе Archicad (2026-09-12) ✅

**Что реализовано:** полный редизайн statuspage: тёмно-синяя шапка с градиентом,
SVG-куб, карточки статистики (красным — конфликты, янтарным — очередь),
пилюли soft/hard, секции с таблицами, пульсирующий баннер фонового синка
с автообновлением, пустые состояния («Так держать!»). Всё inline: стили, лого,
скрипт из 1 строки — внешних ресурсов нет. Эндпоинты и поведение не менялись.

**Что проверено:** 68/68 зелёные (тесты страницы гоняют новый рендер);
скринкаст-превью: `evidence/status-preview.html`.

**Что НЕ проверено:** живой браузер (Chrome/Edge); мелкий экран телефона.

## Шаг A-14. Экран Archicad: зум, диалоги, настройки (2026-09-12) ✅

**Что проверено по спеке Tapir** (скачана `command_definitions.js`, 250 команд,
список — `evidence/tapir-commands.txt`): HighlightElements (наш presence —
формат совпал), Move/Set/GetDetails (ядро — совпали), **FitInWindow**
(зум к элементам, v1.3.1+), **ShowAlert** (диалог с 3 кнопками, v1.5.6+),
GetLayers (слои — только через attributeIds, живой зондаж всё ещё нужен).
Записи выделения (Select) в Tapir НЕТ — «показать» = зум + подсветка.

**Что реализовано:**
- Коннектор: `fit_in_window()`, `show_alert()` → номер кнопки; мок пишет вызовы.
- Страница: «👁 Показать» у локов/конфликтов (зум+янтарь на экране AC);
  «❓ В Archicad» — диалог прямо в Archicad «Моё/Чужое/Позже», выбор пишется
  в журнал (решение принимает человек, не ИИ); «⚙ Настройки» — форма
  accollab.json (секреты: пусто = не менять, числа валидируются).
- CLI `show GUID` (21 команда).
- Макет будущей встроенной палитры: `evidence/palette-mockup.html`
  (честно подписан: настоящая палитра = C++ Add-On, этап DevKit).

**Что проверено:** 72/72 зелёные (+4: зум/диалог в коннекторе, /show+/ask и
/settings на странице); sim 9/9; sim-http 6/6.

**Что НЕ проверено:** диалог ShowAlert живьём (блокирует поток AC, пока человек
не кликнет — таймаут /ask поднят до 10 мин); FitInWindow на «тропе»;
русские буквы в ShowAlert (в спеках — только про char-поле GetPointFromUser).

## Шаг A-15. Пульт настроек: все ручки наружу (2026-09-12) ✅

**Что реализовано:** страница настроек раздута до полного пульта — 7 групп,
19 полей, 3 галки, 3 секрета. Новые ключи конфига: serve_port, relay_timeout,
telegram_timeout, lock_ttl_s, bim_rules, own_color, presence_enabled,
auto_checkpoint, telegram_notify (+ archicad_port наконец в дефолтах).
Новые поведения: выключатель подсветки; авто-чекпоинт перед применением
чужих (только если есть что применять, ошибка не роняет тик); уведомления
о конфликтах человеческим текстом в Telegram-чат (транспорт их пропускает —
фильтр по префиксу, проверено чтением кода); фильтр правил BIM
('DUP_ID,NO_MATERIAL'); свой цвет подсветки; ttl лока и порт страницы
из конфига. CLI lock/serve: флаги перекрывают конфиг.

**Что проверено:** 78/78 зелёные (+6: presence-off, авто-чекпоинт юнит,
уведомления через фейковый Bot API, фильтр BIM, свой цвет, галки+валидация);
sim 9/9; sim-http 6/6. Картинка пульта: `evidence/settings-preview.html`.

**Что НЕ проверено:** уведомление живьём в настоящий чат (формат sendMessage
стандартный, но живой прогон впереди); авто-чекпоинт на большой модели
(скорость снимка); флак test_post_sync_runs_tick (1 падение на 3 прогона
под нагрузкой — дедлайн поднят 15→30с, тест только).

## Шаг A-16. Настоящая страница — во вкладках, как макет (2026-09-12) ✅

**Что реализовано:** главная страница пересобрана во вкладки 1-в-1 с макетом
палитры: Статус / Блоки / Конфликты / Чекпоинты / Настройки. Сообщения,
варнинги и ошибки — общей полосой над вкладками (видны всегда). Вкладки
щёлкаются мгновенно (JS, 10 строк), без JS — обычными ссылками ?tab=...;
после каждого действия возврат на свою вкладку (Показать → назад туда,
откуда кликнули). Форма настроек — та же, что на /settings (общий код).
Ни одна кнопка не потеряна: sync/show/ask/resolve/checkpoint/settings.

**Что проверено:** 79/79 зелёные (+1: вкладки, активная, мусорная, форма
настроек во вкладке); картинка: `evidence/status-preview.html`.

**Что НЕ проверено:** переключение вкладок живым кликом в браузере (JS
тривиальный, но живой клик впереди).

## Шаг A-17. Настраиваемая панель + меню «⋯» (2026-09-12) ✅

**Что реализовано:** логика «часто/редко». Настройки убраны из вкладок —
только колесико ⚙ в шапке (+ /settings прямым URL). Вкладки Статус/Блоки/
Конфликты/Чекпоинты перекладываются: кнопка «⋯ в меню» в заголовке прячет
вкладку в меню «⋯» (открывается кликом, <details> — работает без JS),
«📌 на панель» возвращает. Раскладка — ui.json рядом с конфигом (пустой
список уважаем, битый файл = все вкладки). Убранное доступно из меню и по
прямой ссылке ?tab=... Макет палитры синхронно: настройки там тоже ⚙.

**Что проверено:** 80/80 зелёные (+1: раскладка туда-обратно, меню, прямая
ссылка, мусор); картинка: `evidence/status-preview.html` (чекпоинты в меню).

**Что НЕ проверено:** раскрытие <details> живым кликом; порядок вкладок
перетаскиванием (не делали — только панель/меню).

## Шаг A-18. Триаж внешнего ревью: безопасность демона и страницы (2026-09-17) ✅

**Что реализовано:** внешний патч GPT разобран построчно, годное
переписано по-своему (патч из чата накатывать нельзя — он lossy).
Демон: новый `runtime.py` (атомарные JSON-записи через tmp+fsync+replace,
межпроцессный `sync_guard` — один тик на базу), `Store.transaction()`,
валидация конфига с диапазонами + BOM-tolerant загрузка, привязка базы
к проекту+автору (чужая/занятая база — понятная ошибка, не молча),
живая перезагрузка конфига каждый тик (личность пинится, битый конфиг
не роняет тик), полные GUID локов в статусе, uuid-имена outbox-файлов,
валидация конвертов поштучно (битая операция пропускается, курсоры едут —
без отравления), авто-чекпоинт fail-closed (не создался — чужие не
применяем), heartbeat локов уезжает соседям операцией (throttled:
не чаще половины ttl), `apply_pending` останавливается на ошибке Archicad
с сохранением порядка. Страница: CSRF-токены во все формы, проверка
Host/Origin (анти-DNS-rebinding), лимиты POST, security-заголовки,
воркер /sync держит своё соединение целиком, настройки блокируются во
время синка, кнопка «Чужое» РЕАЛЬНО применяет чужую операцию через
`apply_operation(force=True)` (base check пропущен, отсутствие элемента
и чужой hard-блок проверяются; не вышло — конфликт остаётся открытым).
Отвергнуто из ревью с причинами: гейт Telegram default-off (утверждение
про потерю сообщений неверно для коротких последовательных опросов),
импорт всего батча одним вызовом (отравление курсора → бесконечный
рефетч), RuntimeError при дрейфе личности (пустая база усыновляет новую),
вечный спин run() при занятой базе (теперь выход 1), keep-open кнопки-2
(вместо этого настоящее force-применение). Свои находки сверх ревью:
`cmd_show` падал на отсутствующем Archicad (`scan` не бросает — проверка
None + закрытие базы), двойной `serve_forever` в `cmd_serve` (флаг
background), `UnsupportedOpError` не наследовал `ConflictError` и убивал
весь тик (теперь наследник, пишется в журнал), флак presence (коллизия
цветов через salted hash — теперь первый свободный цвет палитры),
протухание чужих локов через 20с (heartbeat был только локальным).

**Что проверено:** 96/96 зелёные (+16: `tests/test_daemon_safety.py` —
замок, личность, карантин, релоад, heartbeat, fail-closed, force;
тест /ask теперь вставляет настоящую операцию и проверяет применение);
sim_two_machines 9/9; sim_daemon_http 6/6 (ожидание шага 4 обновлено:
heartbeat-лока едет перед комментарием — так задумано); presence 12/12
подряд после фикса флака.

**Что НЕ проверено:** живой Archicad (force-применение и heartbeat между
реальными машинами); msvcrt-ветка замка на Windows (гонялась только
posix-ветка); CSRF/Host — только автотесты, не живой браузер.

## Шаг A-19. Живой тест «письма с сервера» + кит (2026-09-17) ✅

**Что реализовано:** `tools/make_server_files.py` — читает реальный
открытый проект через Tapir, выбирает элементы (сначала балки) и пишет
пронумерованные server-01.json... с движениями от «boris»; чексуммы из
живого проекта, каждый файл двигает свой элемент, вектор проверяется до
записи. `live-test/`: daemon.bat + make-server-files.bat + TEST-ARCHICAD.txt
(BOM+CRLF) + весь src; zip `dist/accollab-live-test-2026-09-18.zip` (59 КБ).
Инструкция: 6 шагов (конфиг → письма → baseline → по одному письму в inbox
+ once → сверка сдвигов в Archicad), доп-тест ручной правки, разбор ошибок,
что прислать. По дороге исправлена неточность: `once` печатает JSON, строка
`tick:` — только у `run`.

**Что проверено:** 97/97 зелёные (+1: test_serverfiles — генерация из мока
и применение 3 писем сквозным демоном, координаты едут, конфликтов нет);
кит прогнан из чистой распаковки (init → письма → baseline → 2×APPLIED →
conflicts пуст).

**Что НЕ проверено:** живой Archicad 29 заказчика (тест впереди); хватит ли
3 балок в его копии проекта.

## Валидация A-19. Живой тест ПРОШЁЛ на Archicad 29.0.0.3000 RUS (2026-09-18) ✅

**Что проверено:** Codex на ноутбуке заказчика прогнал кит по CODEX-PROMPT
против живого Archicad 29 (порт 19724, проект 393 элемента). 3 письма →
3×APPLIED с вектором ровно x=0.5, независимая сверка чексумм из Archicad —
MATCH все три (3E760DF0…, 18AFDD91…, A8F481C3…). Финал: outbox=0,
конфликтов 0, ошибок 0. Артефакты (server-0X.json, status.json, .done.json)
получены и сверены мной: GUID настоящего формата, координаты нативные
(190+ знаков в Y — Archicad точность хранит). Первый в истории проекта
живой прогон «писем» — зелёный.

**Дефекты, найденные живым тестом (фиксил Codex на ноутбуке, код едет сюда):**
1. Эхо: применённое чужое движение следующий тик записывал как локальное
   (я видел это в дыме и ошибочно назвал нормой). Лечение: baseline
   подтягивается при применении (Store.complete_application).
2. Генератор округлял X до 4 знаков — ломало сверку. Лечение: нативная
   точность (внесено мной сразу, строка одна).
3. Applier верил ответу MoveElements без read-back. Лечение: требовать
   успешный результат + перечитывать элемент и сверять чексумму + recovery
   после смерти процесса между движением и записью квитанции.

**Открытые вопросы:** (а) все 393 элемента отчитались как Beam — правда
балки или маппинг типов врёт? (б) интеграция кода Codex: жду zip src+tools
с ноутбука → внесу, покрою тестами, прогоню 98+, запишу шаг A-20.

**Что НЕ проверено:** те же 3 письма на второй машине (двусторонка);
поведение при живом конфликте (ручной сдвиг между письмами).

## Шаг A-20. Интеграция фиксов живого теста (2026-09-18) ✅

**Что реализовано:** код Codex с ноутбука (store.py + applier.py пастой)
внесён НЕ вслепую, а ханками в эталонный код, каждый сверен:
1. `Store.complete_application()` — квитанция + подтяжка baseline атомарно,
   только если baseline ещё равен заявленному before (эхо убито).
2. Applier: явный success от MoveElements (обе формы Tapir) + read-back
   элемента со сверкой чексуммы + recovery already_applied (смерть между
   движением и квитанцией — дописываем receipt, не конфликт).
3. Генератор: округление убрано ещё в A-19 (код Codex не прислан, но
   описание совпадает с моим коммитом один в один — эквивалент).
Адаптации сверх пасты Codex (его код проверен, но не идеален):
- `except UnsupportedOpError` стоял ПОСЛЕ `except ConflictError` — мёртвый
  код (наследник ловится раньше). Переставил первым: статус UNSUPPORTED
  реально работает + убран спам строками конфликтов каждый тик.
- `apply_pending` не ставит квитанцию на UNSUPPORTED (иначе молчаливое
  потребление: ни квитанции, ни строки в журнале).
- Force («взять чужое») при строгом read-back умирал всегда, когда нужен:
  дельта от текущего состояния приземляется не в заявленный after.
  Решение: force = OT-композиция (чужая дельта к нашему состоянию, наша
  правка не затирается), квитанция и baseline фиксируют ФАКТ из read-back.
  Обычный путь строгий как был. Кнопка /ask работает как раньше.
- Семантика: одинаковые конкурентные движения теперь конвергируют молча
  (already_applied), конфликт — только при расхождении. Тест детерминизма
  переведён на расходящиеся операции (намерение сохранено).
Фикстуры тестов приведены к контракту операций ({"type","checksum","details"}):
раньше before/after без checksum работали только потому, что код был lax.

**Что проверено:** 102/102 зелёные (+4: no-echo, read-back-mismatch,
already-applied, unsupported-no-spam; force-тест обновлён под OT-семантику);
sim_two_machines 9/9; sim_daemon_http 6/6. Кит пересобран из нового кода.

**Что НЕ проверено:** force с расхождением на живом Archicad; двусторонка
(вторая машина); смешанный проект (стены/перекрытия, не только балки).

## Шаг A-21. Аудит-2, батч 1: PLN-гард + durable чекпоинты + broadcast + preshift (2026-09-22) ✅

**Что реализовано (все 4 CRITICAL ядра подтверждены чтением кода):**
1. PLN-гард: первый тик привязывает базу к открытому PLN (GetProjectInfo:
   path+name+untitled+teamwork), чужой/неизвестный PLN — fail closed
   (модель не читаем/пишем, транспорты идут). CLI `pln` / `pln --repin`,
   привязка видна в status.json. Безымянный проект привязать нельзя.
2. Durable чекпоинты: схема v3 + таблица checkpoint_data (миграция копирует
   данные старых чекпоинтов, legacy-ссылки читаются как fallback). Ротация
   снимков (keep=20) чекпоинты больше не убивает.
3. Broadcast отката: rollback_apply кладёт компенсирующие операции в outbox.
4. Preshift-проверка applier: after обязан получаться жёстким сдвигом
   (допуск микрон на флоат-пыль), иначе UNSUPPORTED ДО касания модели.
   Плюс best-effort компенсация при провале read-back (движение назад).
   Force-путь без изменений (квитанция по факту).

**Что проверено:** 110/110 зелёные (+8: test_audit2.py — пин/блок/repin,
импорт при блокировке, durable+миграция, broadcast, length-untouched,
компенсация); оба сима зелёные.

**Что НЕ проверено:** PLN-гард на живом Archicad (два открытых PLN);
компенсация на живом (только crooked-mock).

## Шаг A-22. Аудит-2, батч 2: строгий ингест + аренда локов + атомарность стора (2026-09-22) ✅

**Что реализовано:**
1. Строгий validate_op: вход обязан быть dict; проверяются kinds/ops,
   author, lamport-int, JSON-parse before/after/deps/base; project-guard
   сохранён, legacy без project пропускается. wall_time: str для обычных,
   str|int для comment (комменты пишут int).
2. Applier: SKIP-*/UNSUPPORTED не ставят квитанцию (ретраи каждый тик);
   ERROR-op — квитанция + лог (яд разгружен); _auto_checkpoint считает
   только primary и не ест create/delete. self.errors первым в _tick.
3. Watcher: poll_and_record атомарна (снимок пишется только при успехе);
   create/delete фиксируются в истории, но НЕ уходят в outbox
   (report["not_synced"] предупреждает).
4. Store: настоящий transaction() (_tx_depth, вложенность безопасна);
   IntegrityError ловится только UNIQUE, остальное пробрасывается.
5. Locks: apply_remote отвергает badjson/badpayload/zanyato/stale-same-holder
   (same-holder+другой lease = SKIP,heartbeat сам чинит); unlock требует
   совпадения lease; acquire проверяет ttl 1..86400; _expired держит отраву.
   Все коммиты locks/comments идут через _commit (счётчик в тестах).
6. Daemon: sweep раньше heartbeat + ttl-гард; project_id штампуется
   в lock/unlock/comment/reply/resolve CLI.

**Что проверено:** 123/123 зелёные (+13: test_audit2b.py — 12 новых,
test_daemon_safety обновлён под строгий контракт); sim_two_machines 9/9;
sim_daemon_http 6/6. По ходу чинено: квитанции SKIP внутри веток
comment/lock applier (тест поймал), опечатка в отступе теста.

**Что НЕ проверено:** живой Archicad; dual-transport race (известен,
фикс — батч 3); relay-харденинг (батч 3).
