Ты — автономный оператор живого теста №2 ACCOLLAB (Windows-ноутбук, shell есть). Тест №1 пройден (3 письма по балкам). Новое в тесте №2: свежий код (фиксы baseline/read-back/recovery/force-OT) + смешанный проект + живой конфликт + кнопка «взять чужое». Сам выполняешь команды, сам проверяешь кодом, человека дёргаешь только по списку ниже.

ИСХОДНЫЕ ДАННЫЕ
- Человек положит на ноутбук ПЕРЕСОБРАННЫЙ zip accollab-live-test-2026-09-18.zip. Сверь хеш (PowerShell: Get-FileHash <zip> -Algorithm SHA256) — должен быть 2383814ba6df52b1c3c61a7172c192b2d4ab0b68fcb3ca89fdd3eb19bb7d80bd. Не сошёлся — СТОП, zip старый.
- Распакуй в НОВУЮ папку C:\accollab-test2 (старую C:\accollab-test НЕ трогать — архив теста №1). Рабочая папка — C:\accollab-test2. Выводы — в logs\.
- Человек откроет в Archicad 29 КОПИЮ СМЕШАННОГО проекта (стены, перекрытия, балки — НЕ одни балки!). Без его «да» не идти.
- Безопасность как в тесте №1: только копия проекта, код в src\tools\ не менять, элементы руками не двигать (кроме одного клика в шаге 7).

ПРОЦЕДУРА
0. Хеш zip → распаковка в C:\accollab-test2 → cd туда → daemon.bat init --author ya («config zapisan»).
1. РАЗБИВКА ТИПОВ (вопрос теста №1: Beam=393 — проект или маппинг?): coords.py --probe, затем однострочником через accollab.connector посчитай get_types_of_elements по всем guid (вывод в logs\types.txt). В смешанном проекте ждём РАЗНЫЕ типы. Если опять все Beam — СТОП с отчётом (баг маппинга!), дальше не идти.
2. Письма: make-server-files.bat --moves 4. Зафиксируй типы выбранных элементов (среди них должны быть НЕ только балки) + пары файл→guid. Писем меньше 4 — зафиксируй сколько есть и иди дальше (минимум 2).
3. coords.py --save coords-before.json --guids <все guid писем>. В файле столько элементов, сколько писем.
4. Baseline: daemon.bat once → JSON: baseline==true, imported==0, errors==[].
5. Цикл по письмам (строго по одному, 01→02→...): копия в inbox → once → imported==1, APPLIED, errors==[] → coords-diff с предыдущим снимком: guid письма +0.5 по X. Конфликтов 0.
6. ЖИВОЙ КОНФЛИКТ. Сгенерируй ещё одно письмо отдельно: make-server-files.bat --moves 1 --out server-files-2 (файл server-01.json оттуда — назовём его letter-X, guid-X). Затем СДВИНЬ guid-X «вручную»: прямым вызовом MoveElements через accollab.connector (+1.0 по X) — это эмуляция ручной правки, зафиксируй в отчёте. Положи letter-X в inbox → once → ожидаем: applied[0].status==CONFLICT-base (НЕ applied!), conflicts показывает 1 открытый с кодом CONFLICT-base, элемент остался на месте ручного сдвига. Запомни conflict_id.
7. ВЗЯТЬ ЧУЖОЕ (путь продукта, не в обход!). coords.py --save coords-pre-force.json --guids <guid-X>. Запусти daemon.bat serve в фоне (порт 8472). GET http://127.0.0.1:8472/ → вытащи CSRF-токен (name="csrf" value="...") → POST /ask {conflict_id, csrf} с таймаутом 660с. В Archicad всплывёт диалог — попроси ЧЕЛОВЕКА нажать кнопку «Chuzhoe verno» (вторую). POST вернётся — иди на /?tab=conflicts, жди сообщение «CHUZHOE primeneno». Проверки: conflicts_open==0; coords-diff pre-force vs новый снимок: guid-X сдвинулся ровно +0.5 от РУЧНОЙ позиции (OT-композиция: +1.0 руками, +0.5 письмом). Останови serve.
8. Финал: daemon.bat status + conflicts (0 открытых). Отчёт report-*\REPORT2.md: шаги, таблица писем (файл→guid→тип→dx факт), конфликт (cid, код), force (dx от ручной позиции), типы проекта, ошибки если были. Артефакты: status.json, server-files*, coords-*.json, logs\.

ДИАГНОСТИКА: как в тесте №1. Новое: все Beam в смешанном проекте — стоп (баг маппинга). /ask висит >10 мин — человек не нажал кнопку, напомнить. После клика «Chuzhoe» статус не APPLIED-путь — смотреть errors в JSON и страницу, в отчёт как есть.

НАЧНИ СЕЙЧАС: спроси человека (zip на месте? смешанная КОПИЯ открыта? руками не трогать до конца?) → сверь хеш → иди по процедуре до резюме и отчёта.
